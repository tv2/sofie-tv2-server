name: 'Publish Image'

on:
  workflow_dispatch:
    inputs:
      versionTag:
        description: 'Version tag (e.g. 0.0.1-staging)'
        required: true
        type: 'string'

jobs:
  publish-image-to-docker-hub:
    name: 'Publish image to Docker Hub'
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'Retrieve repository files'
        uses: 'actions/checkout@v4'

      - name: 'Log in to Docker Hub'
        uses: 'docker/login-action@v3'
        with:
          username: '${{ secrets.DOCKERHUB_USERNAME }}'
          password: '${{ secrets.DOCKERHUB_PASSWORD }}'

      - name: 'Build and publish Docker image'
        uses: 'docker/build-push-action@v6'
        with:
          context: '.'
          push: true
          tags: |
            tv2media/alba-tv2-server:${{ inputs.versionTag }}

  publish-migrations-image-to-docker-hub:
    name: 'Publish migrations image to Docker Hub'
    runs-on: 'ubuntu-latest'
    steps:
      - name: 'Retrieve repository files'
        uses: 'actions/checkout@v4'

      - name: 'Log in to Docker Hub'
        uses: 'docker/login-action@v3'
        with:
          username: '${{ secrets.DOCKERHUB_USERNAME }}'
          password: '${{ secrets.DOCKERHUB_PASSWORD }}'

      - name: 'Build and publish Docker image'
        uses: 'docker/build-push-action@v6'
        with:
          context: './migrations'
          push: true
          tags: |
            tv2media/alba-tv2-server-migrations:${{ inputs.versionTag }}
